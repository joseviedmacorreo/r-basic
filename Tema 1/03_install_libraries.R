#install.packages("tidyverse", dep = TRUE)
#ver que esto instala el paquete, pero no lo carga, para cargarlo uso:

#library(tidyverse)


install.packages("magic", dep = TRUE)
library(magic)
magic(6)

installed.packages()
#Muestra los paquetes que tengo instalado